你是一个资深全栈架构师与前端体验工程师。

请构建一个基于 LangChain 的 RAG 聊天助手，并使用我本地 skills 目录中的：

skills/ui-ux-pro-max

作为前端设计规范来源。

前端必须严格遵循该 skills 中的 UI 设计体系、组件风格、布局规范、交互动效标准。

最终目标是：
构建一个具有 Apple 风格毛玻璃（Glassmorphism）动态效果的聊天系统。

====================================
一、整体架构要求
====================================

采用前后端分离架构：

backend/
frontend/

后端：FastAPI + LangChain RAG
前端：Next.js 或 Vite + React + TailwindCSS

====================================
二、后端技术要求
====================================

1. Python 3.11+
2. LangChain
3. Chroma 向量数据库（本地持久化）
4. SiliconFlow Embeddings（OpenAI 兼容接口）
5. FastAPI
6. 支持 streaming 响应
7. Conversation Memory
8. 支持文档 ingest API

API：

POST /ingest
POST /chat
GET /health

支持流式响应（Server Sent Events 或 StreamingResponse）

====================================
三、前端 UI 设计要求（重点）
====================================

必须严格按照 skills/ui-ux-pro-max 的设计系统构建。

UI 目标：Apple 类毛玻璃 + 流体动效。

具体实现：

1. 使用 Glassmorphism 设计：
   - backdrop-blur-xl
   - bg-white/10
   - border-white/20
   - shadow-2xl
   - 圆角 2xl

2. 背景效果：
   - 动态渐变背景
   - 轻微粒子动画（可用 canvas）
   - 慢速流动 gradient animation

3. 聊天窗口设计：
   - 居中卡片式布局
   - 半透明玻璃卡片
   - 顶部导航栏毛玻璃悬浮
   - 滚动平滑
   - 输入框浮动阴影
   - 自动高度扩展 textarea

4. 动态效果：
   - 消息淡入动画（fade + slide）
   - 打字机效果
   - AI 回复 streaming 展示
   - hover 微交互
   - 点击 ripple 效果

5. 响应式设计：
   - Mobile 优化
   - 平板适配
   - 桌面沉浸式布局

6. 深色模式优先（默认 dark）

====================================
四、前端工程结构
====================================

frontend/
│
├── components/
│   ├── ChatWindow.tsx
│   ├── MessageBubble.tsx
│   ├── InputBox.tsx
│   ├── GlassCard.tsx
│   └── BackgroundEffect.tsx
│
├── hooks/
│   └── useChat.ts
│
├── styles/
│   └── globals.css
│
├── pages/
│   └── index.tsx
│
└── tailwind.config.js

===================================
五、API 配置
====================================

使用 SiliconFlow API（OpenAI 兼容）：

API Key: sk-wuadnzldpoyhzhgbdxuynrvsxdcfazrawagfnjmgyulvjwek
API Base URL: https://api.siliconflow.cn/v1

.env 配置：
SILICONFLOW_API_KEY=sk-wuadnzldpoyhzhgbdxuynrvsxdcfazrawagfnjmgyulvjwek
SILICONFLOW_API_BASE=https://api.siliconflow.cn/v1

LLM 模型推荐：
- Qwen/Qwen2.5-72B-Instruct
- deepseek-ai/DeepSeek-V3

Embedding 模型推荐：
- BAAI/bge-large-zh-v1.5

====================================
六、RAG 实现要求
====================================

1. 文档加载（PDF/TXT）
2. RecursiveCharacterTextSplitter
3. Chroma 持久化
4. Retriever + LLM Chain
5. 抽象 Embedding 接口
6. 支持未来替换向量数据库

====================================
七、Git 要求
====================================

初始化 Git：

git init
git add .
git commit -m "Initial commit: RAG Chatbot with Apple Glass UI"

创建：

main
dev

.gitignore 必须包含：
.env
node_modules
chroma_db
__pycache__

====================================
八、README 必须包含
====================================

1. 架构说明
2. UI 设计理念
3. 动效实现说明
4. RAG 工作流程图（文字）
5. 如何运行
6. 如何扩展

====================================
九、代码质量要求
====================================

- TypeScript 严格模式
- 所有接口类型定义完整
- 使用 Hooks 管理状态
- 组件可复用
- 不允许硬编码 API Key
- 代码可直接运行

====================================
十、最终输出要求
====================================

1. 输出完整后端代码
2. 输出完整前端代码
3. 输出所有文件内容
4. 不允许省略关键实现
5. 确保运行无报错
6. 提供运行步骤

请生成完整工程。
